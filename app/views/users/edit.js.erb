$('#show-user').hide(1000).after('<%= j render("users/form") %>');
$('.view-cv').fancybox({
  buttons: ['close'],
  transitionIn: 'elastic',
  transitionOut: 'elastic',
  speedIn: 400,
  speedOut: 400,
  type: 'iframe',
  iframe: {preload: false},
  width: '100%',
  height: '100%',
  padding: 0
});

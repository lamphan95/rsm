$(document).ready(function(){
  $('.datepick').datepicker( {
    format: 'dd-mm-yyyy',
    startView: 'months'
  });

  $('.datepick_login').datepicker( {
    format: 'dd-mm-yyyy',
    startView: 'months'
  });
});
